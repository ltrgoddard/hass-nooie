# Changelog

## 0.2.0

- Repository renamed to `hass-nooie`; the add-on now installs the proxy from
  the `proxy/` subdirectory.

## 0.1.0

- Initial release: one preloaded, supervised `nooie-proxy` process per camera,
  served as RTSP/WebRTC/HLS by go2rtc.
